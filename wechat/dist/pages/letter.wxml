
  <view>
    <view class="add imgmargin">
      <image class="add" src="{{add}}" bindtap="letter"/>
    </view>
    <view class="center">
    <view class="size"  wx:for="{{text}}" wx:key="index">
      <text>{{item.letters}}</text>
    </view>
    </view>
  </view>